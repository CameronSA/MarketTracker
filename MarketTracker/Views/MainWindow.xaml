<Controls:MetroWindow x:Class="MarketTracker.Views.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="Market Tracker"
                      Height="200"
                      Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Width="80" Grid.Column="0" Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="FTSE Index:"/>
        <ComboBox ItemsSource="{Binding Path=Model.FTSEIndexes}" SelectedValue="{Binding Path=Model.FTSEIndex}"  Width="80" Grid.Column="1" Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
        <Button Width="80" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Height="40" VerticalAlignment="Center" Content="Start" BorderBrush="Black" Command="{Binding Path=Command}" CommandParameter="btStart"/>
    </Grid>
</Controls:MetroWindow>